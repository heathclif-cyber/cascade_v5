# Cascade v5 — Colab dependencies
# torch + numpy sudah pre-installed di Colab runtime GPU — jangan di-pin ulang
# karena compiled .so extensions akan conflict (ABI mismatch)

# Gradient boosting (LGBM CPU di Colab)
lightgbm>=4.0.0

# Data
pandas>=2.0.0
pyarrow>=14.0.0

# ML utils
scikit-learn>=1.3.0
joblib>=1.3.0

# HTTP (Binance API)
requests>=2.28.0
